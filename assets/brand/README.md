# Seal Harness mascot

Selected candidate: `A1`, the guardian seal direction. It connects the product's Policy,
Approval, recovery, and workspace safety boundaries to a calm, trustworthy baby seal.

- Composition: upright, emerging from the lower-left corner;
- Dimensions: `1254 × 1254`;
- Character colors: warm ivory `#F4E7D3` and deep ink navy `#18253D`;
- Background: muted indigo `#5B63A9`;
- Generator: built-in OpenAI `image_gen` tool; the concrete model name is not exposed by the
  tool schema;
- Constraint delivery: main-prompt constraints.

Prompt subject treatment: one extremely simplified baby harbor seal, a continuous protective
teardrop silhouette, broad rounded muzzle, visible paired front flippers, one broad navy chest
region, calm friendly expression, and readability at `32 × 32`.

Exact constraint text:

```text
Use no text or watermark. Add no borders, frames, cards, presentation masks, symbols, tools,
extra subjects, or scenery. Use no fragile lines, sharp tips, unnecessary outlines, tiny details,
decorative marks, photorealistic material, dramatic bevel, glossy hotspot, deep occlusion,
extrusion, strong three-dimensional rendering, or external cast shadow. Keep the background
solid and uniform, with no texture, vignette, halo, or lighting variation.
```

The same source image is copied into `apps/web/public/assets/` for the packaged Web UI. Keep both
copies byte-identical.
