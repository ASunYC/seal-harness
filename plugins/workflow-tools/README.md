# @seal-harness/workflow-tools

Registers the DSH-compatible `workflow` tool. Model-authored JavaScript runs in an isolated worker with only `agent`, `parallel`, `pipeline`, `phase`, `log`, and `args`. Child work uses the ordinary Session-owned `SubagentService`.
