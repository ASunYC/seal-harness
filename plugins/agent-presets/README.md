# @seal-harness/agent-presets

Pins one composition preset to each Session before its first model request. Presets contribute a system-prompt section and intersect the visible global tool set; only a blank Session can switch.
