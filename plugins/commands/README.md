# @seal-harness/commands

Global slash-command registry. Commands execute directly against a Session without a model round trip and append paired `command.run` / `command.done` lifecycle events, including thrown and aborted outcomes.
