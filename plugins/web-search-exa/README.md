# @seal-harness/web-search-exa

Optional Exa search backend. The API key is resolved for every request through `CredentialService`; normalized sources contain only URL, title, snippet, and publication date.
