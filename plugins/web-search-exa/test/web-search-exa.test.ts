import { describe, expect, it, vi } from "vitest";
import { ExaWebSearchProvider } from "../src/index.js";
describe("ExaWebSearchProvider", () => { it("resolves credentials dynamically and normalizes sources", async () => { const credentials = { resolve: vi.fn(async () => "secret") }; const request = vi.fn(async () => new Response(JSON.stringify({ results: [{ url: "https://a", title: "A", text: "snippet", publishedDate: "2026-01-01" }] }), { status: 200, headers: { "content-type": "application/json" } })); const provider = new ExaWebSearchProvider(credentials, { endpoint: "http://127.0.0.1/search" }, request as typeof fetch); await expect(provider.search({ query: "q", maxResults: 2 })).resolves.toEqual({ sources: [{ url: "https://a", title: "A", snippet: "snippet", publishedAt: "2026-01-01" }] }); expect(credentials.resolve).toHaveBeenCalledOnce(); expect(request).toHaveBeenCalledWith("http://127.0.0.1/search", expect.objectContaining({ redirect: "error" })); }); });
