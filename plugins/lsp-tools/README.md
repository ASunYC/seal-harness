# @seal-harness/lsp-tools

Registers the read-only `lsp` tool. Model coordinates are one-based UTF-16; the provider seam uses zero-based positions. Results are grouped and bounded before entering model context.
