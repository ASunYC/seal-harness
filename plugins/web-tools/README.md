# @seal-harness/web-tools

Registers stable `web_search` and `web_fetch` tools. Multi-query search is concurrent, fail-fast/drained, URL-deduplicated and round-robin merged. All returned provider text is explicitly marked external and untrusted; fetched HTML has active and hidden content removed before bounded text/Markdown rendering.
