# @seal-harness/todo-tools

Registers `todo_write`, a complete-list replacement tool whose latest snapshot is persisted in
the owning Session. The standing list expires when a later turn starts. Configuration selects
whether several entries may be `in_progress` concurrently.
