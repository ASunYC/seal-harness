import { describe, expect, it, vi } from "vitest"; import type { ToolExecutionContext, UserQuestionService } from "@seal-harness/core"; import { askUserQuestionTool } from "../src/index.js";
describe("ask_user_question", () => { it("maps snake-case inputs and renders structured answers", async () => { const ask = vi.fn(async () => ({ answers: [{ id: "mode", selected: ["safe"] }] })); const tool = askUserQuestionTool({ ask } as unknown as UserQuestionService); const result = await tool.execute({ questions: [{ id: "mode", question: "Mode?", multi_select: false, options: [{ label: "safe", description: "Safer" }] }] }, { callId: "c", sessionId: "s", cwd: "/w", signal: new AbortController().signal, reportProgress() {} } as unknown as ToolExecutionContext); expect(ask).toHaveBeenCalledWith(expect.objectContaining({ sessionId: "s", questions: [expect.objectContaining({ multiSelect: false })] })); expect(result.content).toEqual([{ type: "text", text: '{"answers":[{"id":"mode","selected":["safe"]}]}' }]); }); });
