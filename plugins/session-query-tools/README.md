# @seal-harness/session-query-tools

Read-only, workspace-scoped tools for searching prior Session history and inspecting Session/event lineage without activating an Agent.

The plugin registers `session_search`, `session_event_search`, `session_trace`, `session_event_trace`, and `session_event_read`.
