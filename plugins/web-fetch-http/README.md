# @seal-harness/web-fetch-http

Anonymous HTTP(S) fetch provider with public-address DNS pinning, same-origin redirect enforcement, response/character limits, strict text decoding, and no ambient credentials.
