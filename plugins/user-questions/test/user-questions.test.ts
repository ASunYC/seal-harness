import { describe, expect, it } from "vitest"; import { DefaultUserQuestionService } from "../src/index.js";
describe("DefaultUserQuestionService", () => { it("uses the first accepting answerer and snapshots a complete answer", async () => { const service = new DefaultUserQuestionService(); service.registerAnswerer(async () => undefined); service.registerAnswerer(async () => ({ answers: [{ id: "mode", selected: ["Safe"] }] })); await expect(service.ask({ sessionId: "s" as never, questions: [{ id: "mode", question: "Mode?", options: [{ label: "Safe" }] }] })).resolves.toEqual({ answers: [{ id: "mode", selected: ["Safe"] }] }); }); it("rejects empty requests, bad plan intents, and invalid answers", async () => { const service = new DefaultUserQuestionService(); await expect(service.ask({ sessionId: "s" as never, questions: [] })).rejects.toMatchObject({ code: "EMPTY_QUESTIONS" }); service.registerAnswerer(async () => ({ answers: [{ id: "x", selected: ["bad"] }] })); await expect(service.ask({ sessionId: "s" as never, questions: [{ id: "x", question: "?", options: [{ label: "yes" }] }] })).rejects.toMatchObject({ code: "INVALID_ANSWER" }); await expect(service.ask({ sessionId: "s" as never, questions: [{ id: "p", question: "Approve?", options: [{ label: "yes" }], intent: { kind: "plan-review", approve: "yes" } }] })).rejects.toMatchObject({ code: "BAD_INTENT" }); }); });
