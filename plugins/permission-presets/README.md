# @seal-harness/permission-presets

Durable per-Session bundles for sandbox mode and approval policy. Fresh Sessions pin the current default before their first model request; `/permission` reads or switches the effective preset.
