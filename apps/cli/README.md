# @seal-harness/cli

Seal Harness Headless CLI。

```sh
seal-harness --provider deepseek --model deepseek-chat "inspect this repository"
```

默认 Session 写入 `<cwd>/.seal-harness/sessions`。使用 `--config` 可完全替换默认
Profile。
