# @piharness/cli

PiHarness Headless CLI。

```sh
piharness --provider deepseek --model deepseek-chat "inspect this repository"
```

默认 Session 写入 `<cwd>/.piharness/sessions`。使用 `--config` 可完全替换默认
Profile。
