# PI-Desktop 体验对齐

范围：参考 https://github.com/vastsa/PI-Desktop 的体验与桌面分发方式，保留 Seal 品牌、PI 唯一 Agent 内核及现有 Web/CLI。参考项目采用 LGPL-3.0；本轮独立实现同类行为，不复制其源文件或更改其许可。

## 验收清单

当前收尾状态统一见 [交付验收表](./pi-desktop-acceptance.md)。以下为历史增量记录，不代表最新版整体通过。

上述自定义模型发现失败用例独立复跑通过（1 通过、26 未运行）；这只能证明本次独立复跑成功，不证明偶发请求失败原因已修复。

2026-09-15 设置保存的 Windows 短暂占用容错：已有写队列/文件锁下，原子 rename 对 EPERM/EACCES/EBUSY 最多四次尝试，等待 25/50/100ms；不删除目标、不绕过权限、其他错误或非 Windows 立即失败，耗尽保留原错误，原有临时文件清理不变。测试验证等待期间旧文件仍可读、成功替换、上限及不重试的错误；设置文件两文件 19 测试、类型检查、构建通过。不能由此认定前次偶发重命名错误的占用来源已查明。服务联合回归 45 通过、1 失败：本地自定义模型发现 fetch failed（409），不是设置保存断言；仍需独立复核，不声称全套绿色。源码增量未重打 EXE。

2026-09-15 子会话诊断信息整理：系统提示词改为默认收起的只读记录，保留完整文本展开入口，安全文本不执行 HTML；API 提供原始序号作为稳定 disclosure 标识，刷新及历史分页不依赖相对索引。重试记录显示状态、次数、提供方及失败原因。child/history 单测 17 通过；分类和渲染测试通过，服务套件首次遇到设置文件临时重命名失败（500），单独复跑 27/27 通过，未修改设置存储或宣称已修复该偶发现象。真实双子任务 live 场景验证诊断默认折叠、手动展开后刷新保留、思考正文仍实时可见。Web 构建成功，缓存 app 235、child-transcript 10、shell v280。增量尚未进入上一轮 EXE，未提交推送。

2026-09-15 桌面后台退出兜底：startBackend 的 IPC 正常退出宽限仍默认 8 秒，超时改为 SIGKILL，避免 POSIX 后台忽略 SIGTERM 后使 stop 永不完成；只处理当前壳启动的 child，不枚举或结束其他进程。stop 等待真实 exit、重复调用共用 Promise。启动超时通过统一 done 清理就绪监听器，防止迟到消息继续处理。新增不响应 IPC/SIGTERM 的真实 Node 子进程夹具与启动超时检查，桌面两文件六项测试通过。测试在 Windows 执行，POSIX SIGKILL 分支需 Linux CI；不证明工具后代全部被回收。本次 host.mjs 增量未重新纳入上一轮已验证 EXE，避免混淆成品证据。

2026-09-15 Linux 验证链路补齐配置：此前 Ubuntu CI 仅构建并上传 AppImage，现增加 xvfb 下的源码套件以及 AppImage 工具/子会话/长历史三项实际启动检查，失败时上传对应平台报告。平台目标抽为 package-targets，Windows 仍是原六场景；Linux x64 选择真实 AppImage，以官方 extract-and-run 环境变量适应无 FUSE 环境，不关闭 Electron sandbox。后端/UI 指纹检查共用现有逻辑。不支持的平台/架构明确拒绝。发布脚本六项测试和语法检查通过；本机 WSL 仅 docker-desktop，未启动或修改 Docker 内部系统，尚未实际构建/运行 Linux 产物，也未触发远程 CI，不能标记 Linux 验收完成。

2026-09-15 最新 Windows 成品完成：desktop:dist 退出 0，desktop:verify:packaged 六项（解包版/便携版各自的工具、子会话、长历史）全部通过，且结束时复核两个被执行二进制指纹未变。除全部前端资源，报告现包含 core/web/runtime-pi/agent-core/subagent-tools/dsh-compat 六个包的 dist 文件清单和逐文件 SHA-256 对比。便携 EXE 252,664,951 字节，SHA-256 E088633EBB06AE7C02410BD3D488A25E19C5E6B6161123C14A635B229EE5E779；安装 EXE 217,892,778 字节，SHA-256 6B301AA4D45C614EAE41B68C5D1D1EC39B23850D450DD3A85920A7B1F8DD84A1。便携 Authenticode 实测 NotSigned，未声称已签名；单文件冷启动仍慢。此成品包含 app 234 及 PI 步骤持久化、Seal 身份修复。尚未验收实际 NSIS 安装卸载、Linux AppImage、成品付费模型完整工作流；整体对齐仍未完成。未发布或提交推送。

2026-09-15 最新桌面包重建进行中：全量编译及 62 包的干净安装/CLI/SDK/Web/兼容检查成功，自包含 windows-x64 后端 bundle 构建验证成功；desktop:dist 仍在处理桌面 staging，尚未完成 Electron 封装及六项成品检查。当前目录中的 EXE 在本轮封装完成前仍是旧产物，不可作为 app 234 等最新功能的交付证据。版本保持 0.3.4，未发布、未提交推送。

2026-09-15 等待阶段衔接：request_header 为首次 token 前建立当前回合活动项，工具完成后的等待不会继续显示旧回答状态；新增下一请求时只分配一次占位，后续正文沿用该节点。原生工具的完成态提供等待标记，较新的思考/正文覆盖它，仍有工具运行时优先显示工具活动。流错误结束当前过程，移除陈旧实时标记。四个流序列/过程/工具测试文件 30 项通过；实际 Electron tool-progress 在工具输出结束后、模型延迟返回期间明确检测到 waiting，随后完成并清除标记。Web 构建成功，本增量未重打 EXE；兼容工具全部状态和重试倒计时尚未对齐。缓存 app 234、stream-sequence 5、transcript-view 8、tool-card 6、child-transcript 9、shell v279。

2026-09-15 产品身份及完整源码回归：关闭兼容提示词组件默认的 DeepSeek Harness 身份句，legacy includeHarnessIdentity 配置不再改变 Seal 身份；兼容工具、runtime context、自定义 persona 保留。默认及 legacy true/false 测试均覆盖，兼容层 116 项通过；固定模型桌面夹具在每次请求检查不再出现上游身份句，包含真实父子任务。pnpm check 全部成功：140 文件、665 测试通过、1 跳过，Python 6、发布脚本 5 通过。source/report.json 本轮 completed=true，17/17 场景通过，覆盖浅/深色、取消、排队/插话及撤回、图片隔离、失败恢复、工具进度/取消、历史、子任务和实时双子任务。此结果属于当前源码，不更新旧 EXE 的验收结论；未提交推送、未重打包，整体目标尚未完成。

2026-09-15 紧凑过程区新增实时摘要：思考阶段显示最后一行、回答阶段显示正在回答、原生工具阶段显示动作和对象，多个工具运行时显示数量；文本有长度上限且只用 textContent，不插入模型 HTML。逐片段只更新标题，不重新投影或移动正文，不覆盖折叠选择；完成后移除实时标记。新增状态、截断、安全文本和无重挂载测试，过程区与流接入两文件 16 项通过。实际 Electron tool-progress 场景确认收起过程区仍显示命令，工具结束后不再标记运行中。浅色完成态截图已检查；实时态视觉截图、兼容工具摘要和统一等待/重试状态仍待完善。缓存 app 233、transcript-view 7、styles 118、shell v278；未重打 EXE。

2026-09-15 子任务实时预览验收通过：multi-child-live 实际双子任务场景在完成前看到了思考、阶段正文和进行中标记，主任务继续运行，完成后读取持久化子会话。排查发现无 preStep 钩子的 PI 运行不发布步骤边界，导致 agent-core 跳过 assistant.chunk 保存；现所有 PI 步骤发布边界，无钩子时不重写输入。messages API 以独立 liveMessages 返回未完成片段，不改变持久化分页游标；原生与兼容封装均可解析。详情默认展开实时思考，保存用户折叠选择，历史分页移除冻结的实时预览，超限明确提示。运行时、agent-core、服务、预览、历史和渲染六文件 76 项测试通过；实际场景移除诊断探针后再次通过。缓存 app 232、child-transcript 8、child-history 4、shell v277。完整 17 场景统一报告和最新 EXE 尚未重跑；嵌套取消/失败、统一活动摘要、全部工具写入审查仍未完成。

2026-09-15 真实多子代理桌面检查通过：新增 multi-child 固定模型场景，主 PI 会话通过 spawn_agent 工具创建 alpha/beta 两个持久化子会话，再调用 wait_agents；两个子任务均经正常 PI 输出思考和结果。界面验证两条真实记录完成、原始任务文本及结果正确，再点击查看会话，确认实际子会话中保存了思考与回答。首次测试 Profile 缺少 jobService 依赖导致启动失败，补入现有 localJobsPlugin 后复验通过，不修改产品默认 Profile。源码套件现 16 场景，统一报告尚未重跑；此场景不证明运行中的逐字子代理监控、并发时间重叠、嵌套失败与取消全部完成，仍待继续验证。未重打 EXE。

2026-09-15 源码完整回归 15/15：source/report.json startedAt=2026-09-15T08:17:21.417Z、completed=true，包含图片排队及撤回隔离。该报告使用加入子代理任务说明前的构建。另新增原始派发任务：SubagentSnapshot.task 来自创建时持久化 metadata，子代理树展开详情显示安全文本、12,000 字符上限和完整会话入口提示；缺字段的旧会话不虚构任务。持久化恢复及 UI 单测共 9 通过，Web 及依赖构建成功。任务说明增量尚未实际多子代理 GUI 验证，未重打 EXE。缓存 app 231、subagent-tree 2、shell v276。

2026-09-15 队列图片访问隔离：实际桌面图片场景增加独立会话，确认排队中的图片不能通过该独立会话的附件路径读取（404）；新增 queue-image-remove，在移除及重开原会话后分别用 no-store 请求原附件链接，两次均 404，正文/历史没有该消息，模型没有收到后续任务。首次扩展时侧栏第一项变成隔离用会话，暴露测试选择器错误；现按固定测试任务标题定位原会话，撤回及正常图片消费两场景复验通过。源码套件扩为 15 场景，完整统一报告尚未重跑。此检查限制的是会话级引用可达性，不删除已上传文件，也不宣称同一用户的上传存储接口不可读取。尚未重打 EXE。

2026-09-15 带图片追加的真实桌面场景通过：通过文件输入上传固定 PNG，验证排队缩略图、PI 模型收到图片、消费后的会话附件 URL，以及重开同一图片且实际加载成功。首次运行暴露原生 /api/runs/:id/messages 未解析附件导致 500；修复后又暴露缺少稳定消息 ID 导致持久化映射失效、inbox 保存模型图片而非附件引用，以及会话附件路由未识别当前 inbox 引用。现补齐模型投影和消息 ID，inbox 保存 durable 消息，附件授权仅额外检查当前会话 fold 后仍存在的队列引用；队列缩略图兼容 Seal 附件格式。相关 agent-core/queue 测试 17 通过，Web 服务 27 通过，构建和类型检查通过。源码套件新增 queue-image（合计 14 场景），统一 14 项报告尚未重跑；附件撤回后的访问隔离和更多图片场景需继续验证。缓存 app 230、queue-dock 10、shell v275；未重打 EXE。

2026-09-15 排队撤回及完整源码回归：新增普通排队与插话撤回两个实际桌面场景，点击队列移除后等待首轮结束并重开会话，确保待处理消息不进入正文、历史或后续模型请求。固定模型若收到第二轮调用即报错，两项均通过。源码完整套件现为 13 场景，本次报告 startedAt=2026-09-15T08:06:34.927Z、completed=true、13/13 通过。pnpm check 退出 0：139 文件 656 测试通过、1 跳过，Python 6 通过，发布脚本 5 通过。尚需附件、工具间隙插话、真实多子代理及其他产品级差异验收，本批源码尚未重新打 EXE。

2026-09-15 实际追加消息验收：源码桌面新增 queue/steering 两场景，使用固定响应模型经过真实 PI 执行，首轮输出后通过真实表单追加消息；检查待处理时正文未提前出现、实际消费后四条消息顺序及重开一致，模型端同时确认收到追加文字。queue 首次通过；steering 首次因 queueDockItems 仅接纳 queued 导致待处理插话不可见而失败，现已让队列区接纳 steering 并显示插话标记、保留提交回显，修复后实际场景通过。队列/流处理两文件 11 测试通过，Web 构建及类型检查通过。源码套件扩为 11 场景，完整统一报告尚未重跑；取消待处理消息、附件和工具执行间隙插话仍待验证。缓存 app 229、queue-dock 9、shell v274；未重打 EXE。

2026-09-15 前端追加消息接入：运行中提交成功不再提前向正文插入用户气泡，改由 PI user_message 消费事件使用历史消息渲染器呈现。流序列按消息 ID 去重（不同 ID 的相同文字保留），用户消息后分配新的回答气泡，首个回答尚无内容时移除空占位。联动及序列两个测试文件 10 项通过，Web 及依赖构建、全仓类型检查、源码深色真实 PI 写入/回退/重开场景通过。缓存更新为 app 228、stream-sequence 4、shell v273。仍需实际排队、steering、取消排队、附件和历史重开一致性的 GUI 场景验证；此增量未重打 EXE。

2026-09-15 追加消息时间线检查：前端 submit 在运行中 POST 成功后直接附加用户气泡，而 handleStream 未处理 user_message，因此提交顺序与实际消费顺序可能不同。先修复后端持久化出口：user_message 仅在写入历史成功后映射为同一 durable 消息，保留附件引用和来源，不把模型投影图片字节作为会话展示消息；原始 PI 事件不修改。agent-core 13 项测试通过，新增延迟消费的流事件与历史一致、原始投影未变断言。前端去重、消费时定位和真实队列 GUI 验证仍未接入，此源码增量不在上一轮已验证 EXE 内。

2026-09-15 本轮成品六项复验完成：desktop:dist 退出 0；desktop:verify:packaged 退出 0，报告 startedAt 为 2026-09-15T07:56:05 附近，completed=true，解包版和便携版各自的工具卡片、子会话及 80 回合历史全部通过。报告收录全部 78 个前端资源与两个被执行二进制的指纹，结束后重新核对二进制仍一致。便携 EXE 252,657,913 字节，SHA-256 `9EFB3EBC3BA27AC323BFED4D0C201C7D6B50199C055CE14859FA0EB418FCAEB9`；安装 EXE 217,885,204 字节，SHA-256 `A0A74FFC7F730DD4111A68304122F6991F95CD80B851ABBE3BC6C374AF6B23E1`，均 NotSigned。已目视检查本轮工具卡片截图。此报告不覆盖 NSIS 安装卸载、Linux、真实多子代理、所有思考/异常场景或便携内部全部资源逐项比对；便携冷启动解包偏慢仍待优化，整体对齐未完成。

2026-09-15 本轮封装进展：Electron 下载及解包成功，NSIS 已生成并进入 portable 目标构建，整个 desktop:dist 仍未返回。对本轮 win-unpacked 分别实际运行工具卡片、子会话详情和 80 回合历史三个 smoke，均退出 0，页面无 token、无 window.require；这些是独立验证，不是已完成的六项成品报告，也不替代安装卸载或便携 EXE 验收。完整六项报告需等待当前构建完成后运行。

2026-09-15 成品证据加强：验证脚本由 12 个指定前端文件改为递归核对全部资源清单和 SHA-256，发现缺失、多余或内容过期均失败；报告另记录实际执行的解包版与便携版 EXE 大小和 SHA-256。新增资源校验测试，test:release 共五项通过，脚本语法及 diff 检查通过；当前源码与 Web dist 的 78 个资源全部一致。完整打包的 pack:smoke（62 包）和 release:bundle 已成功，桌面封装仍运行中；不能据此声称新版 EXE 已验收，便携版内嵌资源仍需独立验证。

2026-09-15 打包网络故障复核：上一轮 desktop:dist 已确认退出 1，原因是下载 Node.js LICENSE 时 raw.githubusercontent.com 连接超时，现存 EXE 仍为旧产物。新增同版本许可证下载有限重试：首次请求 raw，后续使用 GitHub 官方 contents API 的原文响应；每次 30 秒上限、最多三次，永久 HTTP 错误直接失败，不接受空响应或非许可证正文，不降级为缺少许可证的发布。四项 node:test 通过并纳入 pnpm check；真实网络探测经重试取得 v24.19.0 的 157,601 字符许可证。已重新启动完整 desktop:dist，尚未取得新的成品验证结论。

2026-09-15 再次完整回归：源码九项桌面场景（浅/深色写入回退与重开、取消、失败恢复、工具进度、工具中断、长历史、子会话、工具卡片）全部通过，source/report.json 的 completed 为 true；`pnpm check` 退出 0，139 文件 654 测试通过、1 跳过，Python 6 项通过。已启动新一轮 `pnpm desktop:dist`，运行时代码冻结待成品复验；不能将上一轮 exe 或旧 packaged/report.json 当作本次产物证明。

2026-09-15 执行中工具取消：新增实际 shell 首段输出后点击停止、侧栏重开及延迟写入未发生的桌面场景。首次严格检查发现失败结果丢失部分输出（不能用命令字符串包含同一标记代替结果验证）；PI 工具桥现在为抛错且已有进度的调用保留最近最多 50,000 字符文本，并标注不完整、isError 与中断状态，无进度失败仍沿用原路径。修复后实际桌面检查通过，等待 4.5 秒确认原定延迟写入没有执行。运行时/工具测试 22 通过、1 跳过；源码自动验证扩为九场景，完整九项报告尚未重新生成。仍需真实多子代理、Linux、安装卸载及更全面异常验收，未重打 exe。

2026-09-15 shell 进度链路：workspace shell 将已有有界 stdout/stderr 缓冲按 100 ms 合并发送变化的部分结果，退出时补发最后一份；错误/退出清理待发计时器。新增真实子进程测试验证退出前已收到首段输出。源码桌面新增 tool-progress 场景，经脚本模型 → 正常 PI → 原生 shell → 进度事件 → 卡片，确认首段输出到达时仍执行中，最终结果移除预览并包含末段内容，实测通过。测试仅在隔离 Profile 中允许固定的 Node 输出命令，不变更产品默认权限；源码验证扩为八场景，未重新运行整套八项报告。工具执行中取消及其中间输出持久化仍待检查，未重打 exe。

2026-09-15 工具中间输出：原生卡片增加 progress 接口，工具仍标记执行中，预览明确“尚未完成”、限 400 行/50,000 字符，以安全文本更新同一节点；中间事件按最新部分结果替换，最终结果清除预览，迟到进度不能覆盖完成/失败结果。主会话按 callId 路由 pending 卡片，不重排过程区；未知或已完成调用不创建假卡片。相关三文件 20 项及构建通过。此处仅完成前端接收与展示，尚需真实工具持续发送进度和工具执行中取消的端到端检查，未宣称 shell 实时输出全链路已完成；未重打 exe。

2026-09-15 失败恢复实测：新增受控模型输出部分思考/正文后抛出异常的桌面场景，通过正常 PI/Web 输入启动，验证错误显示、侧栏重开保留部分内容及错误，再提交新消息恢复成功且旧错误仍保留。这是手动后续请求恢复，不是专用重试按钮或自动重试策略。源码七项统一桌面验证全部通过，报告 `.artifacts/desktop-verification/source/report.json` 的 completed 为 true；全量 JS/TS 测试 139 文件、650 通过、1 跳过。仍缺工具中断、真实多子代理等完整验收，未重打 exe。

2026-09-15 取消展示：aborted 完成事件使用“已停止”状态及提示，不再显示 Ready；部分正文保留。源码桌面验证新增 cancel 场景，通过正常输入框启动 PI、等部分思考/正文、点击停止、再从侧栏重开，验证部分内容与停止标记仍在。受控模型响应实际中断信号，未绕过 PI 运行时；测试只使用临时数据。相关两文件 31 项、Web 构建及实际取消场景通过。此场景尚不覆盖执行中工具取消、网络中断或模型失败重试，未重打 exe。

2026-09-15 结构投影焦点：投影前记录会话内焦点，移动节点后以 preventScroll 恢复；若回合结束使控件位于关闭的 details 中，则聚焦最外层可展开 summary，不抢会话外焦点。测试覆盖运行中控件、结束折叠、外部输入以及切回普通视图后的输入选区；焦点/流式两文件 10 项、类型检查和深色 PI 写入/回退/重开通过。首次联合服务回归出现设置文件原子 rename 的 Windows EPERM（与此次 UI 修改不同路径），随后服务文件单独复跑 27 项通过；瞬时文件占用原因未定位，不能据复跑消除该风险。未重打 exe。

2026-09-15 流式投影频率：仅新增思考/回答节点、工具变化及回合结束等结构变化触发过程区投影，普通 delta 不再重复搬动整个过程区。新增 100 段思考加 100 段正文的实际处理函数测试，确认投影次数不增加且 summary 焦点保留；相关三文件 35 项、Web 构建、全仓类型检查和深色 PI 写入/回退/重开通过。Markdown 正文本身仍增量到来时全量渲染；结构变化时的焦点恢复及完整性能基准仍待补，不宣称全面性能优化完成。未重打 exe。

2026-09-15 后台刷新交互保护：`refreshOpenSession` 在请求前与响应后检查会话内焦点、跨会话区域选区及回退确认框；有交互时保留当前 DOM 并延后补刷，切换会话后旧定时器不刷新新会话。三个定时器/DOM 测试覆盖焦点释放补刷、确认框、选区、迟到响应与换会话；加 Web 服务共 30 项通过，构建、全仓类型检查和深色 PI 写入/回退/重开场景通过。此保护针对后台历史刷新，不代表流式投影、显式重开或分页期间的所有焦点场景均已验收；未重打 exe。

2026-09-15 重开会话检查：发现 Profile 未提供消息反馈服务时 `/feedback` 的 501 会阻断历史打开；现在只对此明确不支持状态降级为空反馈，权限/网络/其他服务错误继续抛出。历史工具由回答内嵌改为与实时相同的独立节点，并调整分页插入，避免遗漏工具兄弟节点或因结果更新重排旧卡片。新增降级/错误保留和分页节点顺序测试，相关三文件 33 项通过。桌面场景通过 MutationObserver 捕获实时完成时的节点类型、正文、回合 ID 和过程区归属，再经侧栏重开比较；浅色/深色均通过，80 回合历史场景、Web 构建及全仓类型检查通过。首次新增验证暴露了反馈 501、历史卡片归属和初次自动刷新竞态；交互检查现在等持久化刷新结束后操作卡片，但保留此前实时签名。尚未覆盖逐字思考文本、取消/异常与实时刷新期间焦点保持；本轮源码未重打 exe。

2026-09-15 实时分组接入：流式思考、回答和工具节点绑定执行层提供的真实 turnId，回合结束后使用历史视图同一投影折叠过程；文本回答保留在过程区外，手动折叠不被增量更新强制展开。联动测试覆盖双回合 ID、折叠保留、最终回答及切回普通视图后的内容顺序。浅色/深色实际源码 PI 写入与回退桌面检查通过，新增两个已结束过程区折叠和最终回答可见断言；检查需先展开过程区再操作内部审查按钮。全仓类型检查通过。错误/取消、队列插入消息、完整历史重载一致性和大流式更新性能仍需继续验证；未重新打 exe。

2026-09-15 回合关联后端：持久化执行出口为 `turn_start` / `turn_end` 附加真实 turnId，仅在对应记录成功保存后建立事件映射；不修改 PI 原始事件，不按消费时的当前回合猜 ID。新增延迟消费双回合测试，并在实际 PI 文件写入 Web 接口测试中比对实时起止 ID 与保存记录。重新构建后两文件 40 项通过，全仓类型检查通过；首次接口测试因包导出读取旧 dist 失败，构建后复验已通过。前端分组尚未消费这些 ID，最新 exe 仍不含此源码增量。

2026-09-15 后续源码增量：实时序列现在处理 PI `turn_start`，新回合直接输出文字时不再拼接到上一回合；无内容回合不生成额外气泡。序列、实际 app 处理函数及 Web 服务三个测试文件 31 项通过，Web 构建及深色源码 PI 写入/审查回退桌面场景通过。尚未将真实持久化 turnId 贯穿实时分组，不能称为完整时间线一致性完成；此增量不在下述已验证 exe 中。

2026-09-15 新一轮 `pnpm desktop:dist` 已退出 0。便携版 252,653,874 字节，SHA-256 `8AE0F46273428E2D9D0C628134E2A1FBC1EE588B6F892FCFA65C4E5AF65E5ECF`；安装版 217,882,091 字节，SHA-256 `268F556C0476C850AAD1DE5D4E210929840685964C428A84AB10D8E5D2362AD7`。两者均 NotSigned。六项成品验证启动时包内 12 项前端文件与源码哈希一致；`pnpm desktop:verify:packaged` 最终退出 0，解包版和便携版各三项全部通过，报告位于 `.artifacts/desktop-verification/packaged/report.json`。便携版解包启动等待较长，仍需体积与启动体验优化；六项通过不替代 NSIS 安装卸载、Linux、真实多子代理以及全部交互验收。

本轮成品复验：`desktop:verify:packaged` 已扩展为解包版、便携版各三个场景（工具卡片、子会话详情、80 回合历史窗口），共六项；脚本语法检查通过。流式顺序、子会话更新、历史窗口联动四个测试文件共 14 项复验通过。新解包版三个场景逐项直接运行均退出 0（工具卡片 3 张、子会话工具卡片 1 张、历史场景完成最早回合定位），页面无 token、无 window.require。完整打包仍在进行，便携版及六项统一报告尚未取得；这不代表安装/卸载或 Linux 验收已完成。

- [ ] Electron 开发启动、单实例、自动鉴权、异常提示与后台退出清理（实现中，待真实窗口验证）
- [ ] Windows portable 单文件 exe、NSIS 安装版；无需用户安装 Node
- [ ] Linux AppImage 与原有 headless/CLI 并存
- [ ] 用户数据默认 `~/.seal-harness`，资源解包目录不存用户数据；既有工作区数据不自动迁移
- [ ] 语义化工具卡片：命令/输出、文件、搜索匹配、修改差异、状态、耗时
- [ ] 思考与工具活动按回合时间线展示，运行中展开、结束后折叠
- [ ] 子代理父子调用树、状态、模型、耗时、详情及失败原因
- [ ] 消息关联的文件修改审查与持久化快照；回退前校验当前文件，冲突不覆盖
- [ ] 长会话有限挂载窗口、加载历史保持滚动位置、定位导航
- [ ] 统一空态、错误态、按钮与浮层主题，以及键盘/无障碍检查
- [ ] 单元/集成测试、实际 GUI 检查和干净安装产物冒烟

不能用“代码存在”代替功能验收，也不能用其中一项完成代表整体完成。

## 已验证进度（2026-09-12）

- 新增 Electron 独立窗口入口与 `desktop:dev`，继续使用现有 Web 后台和 PI 内核。
- 路径与导航单测通过；后台实测完成 401 → 303 Cookie 登录 → 页面加载 → IPC 正常退出与端口关闭。
- Electron 源码窗口冒烟通过：实际页面加载、模型选项就绪、兼容 UI 模块就绪、地址无 token、渲染进程无 `require`。
- 桌面/路径/Web 服务相关 29 个测试通过。完整产物与 Linux 尚需完成验证，未勾选整体完成。
- 全量检查通过：127 个测试文件，577 个测试通过、1 个跳过；Python 6 个测试通过。
- Windows `win-unpacked/Seal Harness.exe` 实测启动通过，使用资源内置 Node，无需系统 Node。NSIS/portable 压缩与最终单文件启动验收仍在进行。
- 已确认当前 exe 未签名；品牌图标、运行时依赖体积审查、Linux 实机验证仍是桌面分发验收项。
- 体积审查发现 `dsh-compat -> dsh-subagent-claude-code/codex` 带入 Claude（约 322 MB）和 Codex（约 284 MB，另有辅助程序）。默认桌面路径未启用这些外部子代理，但其显式兼容配置和测试仍存在。下一步需要核对 PI 唯一内核边界并隔离/清理这些非必需分发依赖，不能仅从包里随意删文件。

## 桌面开发与构建（新增，验收中）

### 第三轮进度

- 2026-09-15：新增统一桌面验收命令与 Windows CI 门禁。源码套件依次验证浅/深色真实 PI 写入回退、80 回合历史、子会话夹具、工具卡片，5 个场景本机均通过，分场景截图和报告保存在 `.artifacts/desktop-verification/source`。打包套件校验 12 个关键 UI 文件与源码哈希后，验证目录版及便携入口；CI 在构建后执行，报告即使失败也上传。此次尚未运行新的打包套件（旧包缺少最近增量，应先完整重建），Linux GUI 和 NSIS 安装/卸载仍待验收。

- 2026-09-15：子会话后台刷新在请求完成时再次校验阅读状态，防止请求期间开始选中/滚动后被迟到快照覆盖；丢弃时不更新指纹，下次允许刷新仍可获取同一新内容。SSE open/reconnect 通知触发一次合并补读，关闭时移除该监听。子历史和 Web 服务共 35 个测试通过（包含真实 session.appended 传输测试），Web 构建通过。完整多代理 GUI 验收仍未完成。

- 2026-09-15：子会话只读详情开始复用 `/api/events` 的 session.appended 通知，150 ms 合并突发变化，10 秒兜底读取；按 sessionId 过滤，关闭清理监听及计时器。请求进行中收到通知只补一次读取，真正发起补读前重新核对历史阅读/滚动/选区状态。6 个控制器测试覆盖通知过滤、合并、忙时补读与清理，Web 构建通过。此处更新的是持久化消息快照，不是子代理 token 级流，真实多子代理 GUI/断线重连验收仍待完成。

- 2026-09-15：新建用户消息气泡改用主题背景、前景和边框 token，消除浅色模式黑底黑字。桌面冒烟新增显式 light/dark 模式和实际 color-scheme 校验，并在真实 PI 提交流程中计算用户正文与合成背景的对比度，低于 4.5 即失败。两种主题下写入/审查/取消/确认回退链路均通过；这不是全页面无障碍认证，也未证明任意第三方皮肤。CSS 版本 117，尚未重打 exe。

- 2026-09-15：新增 `SEAL_DESKTOP_SMOKE_STREAM=1` 源码桌面验收。通过真实输入框提交、本地脚本模型驱动 PI loop、真实 write_file、流式 UI、快照 GET、界面二次确认和回退 POST，验证助手/工具/最终回答节点顺序、Markdown、实际修改内容、取消确认可重新打开、成功提示以及退出后磁盘恢复原始文件。所有文件仅位于冒烟临时工作区，清理不触碰用户项目。fixture-stream 不进入发布资源；明确拒绝使用此开发夹具验证 packaged binary。普通远端模型、多子代理并发和失败/重试 GUI 仍待验证；截图另外暴露新建用户消息气泡浅色主题仍有固定深色背景，需继续统一。此轮没有重新打包。

- 2026-09-15：全量 `pnpm check` 通过（137 个测试文件，633 通过、1 跳过；Python 6 通过）。随后加强真实 PI 写文件/审查端到端用例：经 `/api/runs` 验证思考 → 说明 → 工具调用 → 工具结果 → 思考 → 回答的事件顺序，并确认持久化消息保留两段独立回答，既有快照/重启/冲突/回退断言继续通过。Web 服务 27 个测试复验通过。此测试使用脚本模型驱动真实 PI runtime，并非远端供应商调用，也不替代完整真实流 GUI 验收。

- 2026-09-15：实时主会话修复“工具之后的回答继续写入工具之前的首个气泡”的顺序错误。新增流式阶段控制器，工具批次之后或正文再次转思考时创建新助手节点，连续 delta 不重复建节点；工具节点统一放在导航浮层前，避免后续回答插到工具前。完成时收起本次流的各段思考。3 个控制器/实际 handleStream 函数联动测试验证顺序及结果分离，Web 构建通过。仍需真实 PI 多步流 GUI、失败/重试分支以及与持久化回合过程区的完整一致性验证；不是完整调用链已对齐。

- 2026-09-15：修复长会话截图中浅色主题的选中会话、代码块普通文字、状态面板背景/标题/预设名称/下拉框配色；这些表面改为读取主题 token，常见代码 token 按 color-scheme 分配对比度更高的配色。Web 构建与 80 回合 Electron 历史冒烟再次通过，截图确认普通代码文本和标准模式名称可读。CSS 缓存版本更新至 116；仍需深色/第三方皮肤与其他弹层的全面回归，不宣称全局主题验收完成。尚未重打 exe。

- 2026-09-15：修复开发用长会话夹具错误使用 CommonJS 条件解析纯 ESM core 的问题。新增持久化 80 回合桌面场景，实测通过侧栏打开会话、真实 Markdown 渲染、加载历史前后锚点偏差不超过 8 px、返回最新回收至约 60 条、缓存恢复不再次请求 messages、跳转最早回合后再回到最新。夹具只写临时数据并随冒烟清理，不使用用户会话。最初超时来自未模拟向上阅读（跟随状态仍开启），补入真实滚动并等待采样后通过。6 个窗口相关测试复验通过。截图另发现浅色主题下选中会话文字、代码块普通文本对比度不足及侧栏深浅样式混用，主题验收仍未通过；此处也不证明实际模型流式过程或极端长回合性能已完成。

- 主会话已接入历史挂载窗口：默认最近约 60 条（边界保留完整回合），更早按钮优先恢复已加载但未挂载的原始消息，再请求下一页；返回最新时释放旧 DOM 并清理既有工具/复制控件。阅读历史期间刷新保留范围，收到实时事件或追加新节点后禁止从旧快照裁剪。回合跳转可继续穿过本地缓存窗口。6 个窗口与实际 app 协调函数联动测试覆盖缓存恢复、远端分页、重复裁剪与新内容保护；此前服务/会话/窗口 35 个测试通过，源码桌面启动冒烟通过。联动测试使用最小消息渲染器，不替代长会话真实 GUI 的 Markdown、滚动锚点和实时场景验收；初次 15 条渐进挂载及单个超大回合优化仍待补齐。此增量未重新打入 exe。

- 搜索工具结果新增按文件分组和行号展示，正文按文本安全渲染；200 条预览上限明确报告余量，混入诊断或不符合原生格式时回退完整原始预览而非丢行。工具/子会话 14 个测试通过，源码 Electron 三类卡片冒烟通过（含搜索分组断言）。前端缓存已更新。本次 UI 增量尚未重打便携包，前一轮 exe 不包含此增量。

- 本轮完整 `pnpm desktop:dist` 已退出 0。新便携 exe（252,627,946 字节）实际解包、启动、审查卡片渲染与退出冒烟通过；SHA-256 为 `511602244A3931ACE3C03A3AA0451830313F6B47BC93E40D21767273D9FB8E3C`。安装版为 217,718,552 字节，SHA-256 为 `F41E72104D74AE15D43FE28D33CC3B1F75402DFBBBAA74D6E33842B8C16C6E76`。两者 Authenticode 状态均为 NotSigned；不能把 builder 的 signing 日志视为已签名。NSIS 实际安装/卸载、Linux AppImage 和完整模型交互验收仍未完成。

- 完整重建中的新 `win-unpacked/Seal Harness.exe` 已通过实际启动和审查/子会话渲染夹具冒烟。包内 `app.js`、`styles.css`、`tool-card.js`、`review-view.js`、`child-transcript.js`、`transcript-view.js` 与当前源码 SHA-256 均一致。审查截图确认增删行与行号可读；这些夹具不证明真实模型到 UI 的全部链路。子会话冒烟退出时出现 Chromium GPU 日志，未影响断言与退出码，仍需普通可见窗口回归。NSIS/portable 此时仍在构建，旧单文件不可作为本轮最新产物验收证据。

- 主会话实际修改面板新增回退按钮与二次确认，明确新建文件删除影响；取消不请求、提交时禁止重复、只接受明确成功结果，错误保留可重试。子任务预览保持只读不提供回退。UI 单测覆盖取消/重复/冲突；真实 Electron 确认交互端到端仍待验证。

- 回退服务与 POST `/api/sessions/:id/reviews/:snapshotId/rollback` 已接通：沿用鉴权/会话快照关联校验，要求 confirm:true，拒绝活动会话/后台任务/后代任务/终端，执行现有 PolicyService 的 workspace-write 决策（非 allow 不执行）。真实 PI 写入链路测试覆盖确认缺失、活跃会话、策略拒绝、内容冲突、成功回退与重复请求。尚未加入界面确认按钮；策略 ask 暂时拒绝，不自动绕过审批。

- 内部回退新增写前意图日志，重试可识别“字节已恢复但完成状态未落盘”并只补记状态。锁记录持有 PID，仅在系统明确报告该进程不存在时回收，回收动作也互斥；活跃、PID 复用、权限不明或缺少 owner 信息均保持 busy。13 个快照测试通过（含真实退出子进程锁恢复）。无法识别的旧锁/恢复锁仍需人工处理，未无条件按超时删锁；用户可见回退入口仍未开放。

- 原生写入与内部回退新增按规范化文件路径的进程内串行队列，覆盖不同会话对同一文件的操作；替换在获得队列后重新核对准备时读取的内容，拒绝陈旧编辑。并发测试验证回退等待写入后拒绝覆盖新内容，失败释放队列。该锁不约束外部编辑器/其他进程，也不替代回退哈希检查；异常退出恢复、API 确认和权限链路仍待实现。

- 新增内部回退存储操作：校验原工作区、常规文件/非重定向路径、当前 after 哈希；恢复原始 UTF-8 字节或移除未变的新建文件，保留原快照并持久化 rolled-back。同一快照用目录锁防止重复并发操作，重复完成请求不再碰文件。9 个快照测试通过。尚未暴露服务/API/按钮：跨写入互斥、外部进程竞态限制说明、崩溃恢复及确认/权限边界仍需补齐，不能宣称完整安全回退已完成。

- 新增真实 PI 运行链路测试：脚本模型通过 `/api/runs` 调用 `write_file`，真实文件变更、工具记录中的快照 ID、消息页面元数据与审查 API 均验证通过；服务关闭重启、用户再次编辑文件后仍读取原快照。另用 Electron 展开审查卡片验证行号、红绿增删和完整快照入口（渲染夹具，不是实际模型调用截图）。构建/类型检查通过。

- 主会话/子会话原生工具卡片新增按需“查看实际修改”，使用只读快照接口展示带行号的连续替换差异与懒加载完整前后文本；预览 400 行上限明确提示。冲突记录显示未确认应用警告，错误可重试，超限快照明确不可用。尚需真实写入到 UI 的端到端与截图验收，未提供回退按钮。

- 快照开始同时保存修改后的内容，新增 ReviewService 与只读 `/api/sessions/:id/reviews/:snapshotId`。读取校验会话归属、UUID、目录边界、大小、SHA-256、文本编码，以及会话工具完成事件中的快照关联；旧快照缺少 after 文件时返回不可用，不从当前工作区补造。审查 UI 与回退仍待接入。

- 默认 Profile 的 `write_file` / `replace_text` 已接入写前原始字节快照，存入数据目录 `review-changes`，按会话哈希/随机快照 ID 分区。元数据关联工具调用并记录 prepared/applied/conflict 与前后 SHA-256；写后读取核对，不匹配不标记已应用。单文件快照上限 2 MiB，超限明确返回不可用。写失败保留 prepared 快照以供排查。单测覆盖这些状态；尚未开放审查查询/回退 API，未完成清理配额、回退并发与用户确认链路，也未覆盖 shell/兼容工具写入，因此该项仍未完成。

- 子会话最新页每 2 秒轮询更新；阅读更早页、向上滚动或选择文字时不自动刷新，手动刷新返回最新。关闭后清理计时器并忽略迟到响应。独立历史控制器覆盖请求串行、分页游标、失败重试、关闭竞态及不变内容免重绘；真实子任务流式事件仍需端到端验证（当前是消息快照轮询，不是 token 级流）。

- 子任务正文/思考复用 Markdown 渲染器，内嵌栅格图片与子会话作用域附件下载可用；工具合并结果保留媒体。子会话与 Markdown 共 10 个测试通过。真实 Electron 渲染夹具发现并修复浅色主题下弹窗黑底黑字、宽度被覆盖问题，重新截图可读。该夹具验证渲染，不替代真实子任务运行、分页和实时刷新验收。

- 子任务只读详情新增“加载更早消息”，加载失败保留已显示内容。思考与正文分开展示，基础工具调用/结果合并成语义卡片；未知工具与孤立结果保留。长文本提供显式展开。尚缺完整 Markdown/媒体、实时增量刷新和分页交互 GUI 验收，不能视为完整子会话体验已完成。

- 会话紧凑视图将最终回答的思考移入过程区末尾，普通视图恢复原始位置；保留原 DOM 节点及复制控件，避免重复投影丢失交互。仅改变展示，不改写持久化消息。完整流式思考/工具统一时间线仍未完成。

- 子任务状态接口返回完整后代树；停止操作按祖先所有权检查，支持停止孙级任务。树展示状态、模型、持久化起止时间计算的耗时、结果和错误。
- 主任务运行时可只读预览子任务最近消息；仍不是完整嵌套实时会话，分页、完整工具渲染和思考时间线仍待补齐。
- 运行中的调用树定期刷新并保留展开项，过期会话状态请求不再覆盖当前会话。
- 会话过程折叠区刷新保留手动展开/收起选择；仅在运行转完成时自动折叠一次。对应会话展示、工具卡片、调用树共 11 个单测通过。
- Windows portable 改用 electron-builder 的 `portable.useZip` 压缩路径，候选文件 252,550,699 字节。实际单文件启动冒烟通过（页面加载成功、无 URL token、渲染页无 Node 权限、正常退出）。此前默认压缩路径测试超时。
- 此便携产物使用上一轮暂存的后端/UI，不能作为最新调用树与折叠修复的交付包；最终仍需完整重建与验收。NSIS 安装和 Linux AppImage 实机验收尚未完成。

### 第二轮进度

- 移除 Claude Code/Codex 外部 Agent 提供方依赖与执行入口；旧配置启动时明确拒绝。泛用 ACP 传输兼容仍保留，其他独立兼容宿主路径还需继续边界审查。
- 精简后 Windows 便携候选包约 207 MB（此前约 365 MB）；首轮大包单文件测试超时，不能视为通过。最终更新后的便携包仍需复验。
- 新增 Seal 原生工具卡片，覆盖读/写文件、替换文本、目录、文件列表、搜索和 shell，合并调用与结果。支持 stdout/stderr 分区、错误和退出码、截断提示、400 行/50,000 字符 UI 预览限制；未知工具继续由兼容插件渲染。
- 修改文本只标注“请求的替换前/后”，不伪装成已经验证的文件差异；持久化审查/回退仍待实现。
- 修正浅色模式正文、按钮、输入区、工具卡片对比度。Electron 离屏截图验证三类展开卡片，完整主题/交互回归仍待完成。
- 全量检查：128 个测试文件通过，584 个测试通过、1 个跳过，Python 6 个通过。
- 最新 UI 与壳测试代码在首轮打包期间有增量调整；最终产物必须重新打包并核对资源哈希，不能交付旧快照。
- 已重新装包：解压目录版实际检测通过，包含三类工具卡片；包内 `app.js` / `styles.css` 与当前构建哈希一致，确认不存在 `@openai/codex` 和 `@anthropic-ai/claude-agent-sdk`。单文件便携版复验进行中。

- `pnpm desktop:dev`：构建 Web 及其依赖，打开 Electron 窗口。
- `pnpm desktop:dist`：打包并验证现有运行时，内置独立 Node，再构建当前平台桌面产物。
- 产物目录 `.artifacts/desktop`。Windows 目标包含 portable 和 NSIS；Linux 目标 AppImage，需在对应平台验证。
- Electron 渲染进程禁用 Node，启用隔离和 sandbox；后端仅监听随机 loopback 端口，通过原有 token 换取 HttpOnly cookie。
- 桌面退出通过父子 IPC 请求后台关闭，不依赖 Windows 的 POSIX 信号语义。
- 尚未签名，不承诺没有 Windows SmartScreen 提示；不自动导入或删除原来的工作区数据。
# 搜索正文定位增量（2026-09-15）

会话搜索结果现在可在打开会话后匹配已渲染正文；有远端内容摘要的结果会按需加载较早历史。匹配支持跨 Markdown 行内节点、按字面量查找，不重写正文 DOM；定位会展开祖先过程区、聚焦并滚动到内容，支持 CSS Highlight 的浏览器会标出命中文字。会话切换、运行开始或分页不前进时停止查找。仅标题/工作区命中不扫描全部历史；正文找不到时明确提示，不伪造消息定位。

当前接口仍只提供会话与摘要，因此这是正文首次匹配定位，不是服务器消息 ID 精确定位；也尚无上一个/下一个命中导航、Markdown 隐藏 URL 的源映射和全面长历史性能验收。四个相关测试文件 41 项通过，包含实际 app 定位协调函数的展开、焦点、滚动检查；JS 语法及 diff 检查通过。尚未进行此次增量的真实 Electron 视觉验收或重新打包 EXE。
