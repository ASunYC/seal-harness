# PI-Desktop 对齐交付验收表

更新时间：2026-09-15。此表替代用增量日志推断整体完成度；历史证据仍见 pi-desktop-alignment.md。

目标不变：借鉴 PI-Desktop 的桌面工作台、会话体验和 EXE 分发；Seal 保持独立品牌、PI 唯一 Agent 内核、Web/CLI 与 DSH 皮肤/工具兼容。没有用户同意，不将未完成项改成暂缓，不提交、推送或发布。

状态：**部分**=已有代码但缺能力或完整验证；**待验**=已有实现，缺当前版本交付证据；**待补**=已确认缺口。下列 24 项是当前已识别的收尾单元，不是对“所有优点”的穷尽证明，也不是 24 个文件或缺陷计数。全部仍未达到最终交付签收。

| ID | 验收单元 | 状态 | 通过条件 / 证据 |
|---|---|---|---|
| A1 | 品牌与唯一 PI 内核 | 待验 | 审查默认入口和兼容开关；正常与子代理请求均无 DSH Agent Loop 或上游身份注入 |
| A2 | Web/CLI/桌面共存及用户目录 | 待验 | 三入口可启动，数据默认 ~/.seal-harness；包解压位置不存用户数据 |
| B1 | 思考、工具、回答时序 | 部分 | 实时与重开记录一致；回合、失败、取消、队列消息顺序均验证 |
| B2 | 完整运行阶段 | 待补 | 等待模型、准备、重试、压缩、恢复、等待子任务由真实运行事件驱动；结束清理 |
| B3 | 折叠与思考摘要 | 部分 | 手动状态、耗时、焦点及切换刷新稳定；不声称模型未返回的私有思考 |
| B4 | 原生及兼容工具卡片 | 部分 | 参数、输出、状态、错误、附件、中间进度及耗时一致；未知工具不丢结果 |
| C1 | 子任务调用树及详情 | 部分 | 委派调用对应真实子会话，模型/任务/状态可见，单任务与并发呈现一致 |
| C2 | 子任务实时进展 | 部分 | 父会话等待摘要跟随子任务动作，详情流式刷新不抢阅读位置 |
| C3 | 嵌套失败与取消 | 待验 | 实际父子孙任务取消、失败、等待收敛；无挂起状态或遗漏工具后代 |
| D1 | 搜索精确定位 | 部分 | 当前正文匹配已实现；补服务端消息定位、源内容匹配，明确无命中语义 |
| D2 | 搜索命中导航 | 待补 | 上一/下一命中、数量、键盘操作，跨历史窗口定位 |
| D3 | 长会话阅读性能 | 部分 | 大历史加载、释放、锚点和用户选区；量化 DOM/渲染开销，不只小夹具 |
| D4 | 输入与排队 | 待验 | 编辑/移除/加速/引导及图片在真实 PI 消费、持久化、重开中一致 |
| E1 | 全面主题一致性 | 部分 | 深浅模式下设置、状态栏、工具、弹层、搜索均无错误底色/不可读控件 |
| E2 | 文案与键盘可用性 | 部分 | 本地化、空态/错误态统一；焦点顺序、逃逸关闭及按钮标签检查 |
| E3 | 桌面工作台交互差异审查 | 待验 | 对参考项目文件/预览/设置/命令入口逐项核对，记录未覆盖差异，不从本表遗漏推断完成 |
| F1 | 修改差异展示 | 部分 | 真实快照支持分散修改的可读多段差异，不以请求参数冒充实际修改 |
| F2 | 回退与存储边界 | 部分 | 兼容工具/命令写入覆盖说明、冲突不覆盖、快照保留策略和异常恢复验证 |
| G1 | 桌面退出与单实例 | 待验 | 就绪超时、正常退出、强制退出、后台/工具子进程清理和第二实例实际验证 |
| G2 | Windows 便携 EXE | 部分 | 最新源码打包、指纹一致、干净环境启动、用户数据持久化；记录体积及冷启动耗时 |
| G3 | Windows 安装版 | 待验 | 实际安装、启动、升级/卸载；用户数据保留与路径验证，不以构建成功代替 |
| G4 | Linux AppImage | 待验 | Linux 实际构建/启动/退出和数据目录；现仅有 CI 配置，不算已验证 |
| G5 | 最新版本完整回归 | 待验 | pnpm check、源码桌面套件、当前产物套件全部通过且与待交付代码指纹对应 |
| G6 | 发布说明与分发约束 | 部分 | 启动文档、版本一致、未签名说明、许可/依赖文件、产物清单；发布本身需用户授权 |

## 执行与报告规则

### 本次证据

B2 兼容压缩事件接入：从已安装 DSH compaction-basic 源码确认 start/end 均携带 compactionId，失败 end 带 error。Web 复用 session.appended，按 Seal StoredSessionEvent.sequence 去重、按 ID 配对重叠压缩、隔离当前会话；未知结束和重复事件不改变状态，失败不误报 Ready。新增 compaction-activity 模块测试以真实 StoredSessionEvent 类型约束夹具。与 Web 服务联合 31 项通过，失败状态调整后模块 2 项及 typecheck 再通过；尚未实际触发 DSH 压缩并贯穿 SSE/桌面验收，断线重连时恢复仍缺。没有引入新的 Agent Loop，未重打 EXE。

B2 长历史 LLM 压缩服务链路：新增真实 HTTP → AgentCore → LlmCompactionService → ModelService → 持久化摘要 → PI 回答集成测试。预置 80 条持久化历史，检查压缩 started/finished 在执行 started 之前、一次摘要调用不带工具、PI 收到摘要及保留尾部而非旧全文、context.compacted 恰好一条、Web 历史可读取摘要。第二次继续对话低于阈值，无压缩通知/额外摘要请求且成功回答。模型输出为本地脚本，非付费外部模型；压缩算法及 Agent/持久化/HTTP 为产品实现。两个文件 37 项通过、全仓类型检查通过。此证据加强服务链路，不等同于真实 LLM 长历史的 Electron 视觉验收，DSH 兼容压缩仍未统一。

B2 / D4 桌面启动取消专项：新增 startup-cancel 场景并纳入源码套件（现在 18 场景，整套新报告尚未重跑）。隔离 Profile 的受控压缩服务在真实 AgentCore prompt 中等待取消；桌面实际上传 PNG、提交、观察压缩状态、点击可见停止按钮，验证输入文字及唯一图片缩略图保留、临时消息移除、按钮恢复提交。后端在收到 AbortSignal 后写隔离目录标记，smoke 校验该标记。无附件和带图片两次运行均退出 0，最终截图已检查，语法及 diff 检查通过。不是实际 LLM 摘要或阈值测试，不能替代真实长历史压缩场景；未重打 EXE。

B2 启动取消：submit 持有启动请求 AbortController；没有 runId 时停止中止 fetch，已有 runId 时继续调用 DELETE 并读取运行终态。启动流在 started 前收到 error 现在抛入原输入/附件恢复路径，不再误走 accepted；主动取消显示保留输入而非运行失败。新增实际 cancelRun 函数测试，以及真实 HTTP 连接中止传到受控压缩服务 AbortSignal 的测试，两文件 30 项通过；Web 构建、typecheck、diff 检查通过。真实桌面压缩中点击停止及附件恢复尚待验收，不用运行中取消场景替代该证据。

B2 Web 启动压缩传输：Web 请求显式 startupProgress=true 时，真实压缩通知打开 NDJSON 并发送 startup_activity；发生于 runId 创建前，客户端状态栏显示压缩/准备，started、运行事件和终态移除临时标记。未启用通知保持原 HTTP 错误，已开始流的压缩错误返回 error 行并结束；断连监听前移到 prompt 前。新增真实 HTTP 加受控压缩服务测试及实际前端处理函数测试，两个文件 38 项通过，Web 构建、全仓类型检查和 diff 检查通过。第一次测试失败是夹具漏配 scripted model，已补齐后复跑。尚无真实长历史 LLM 压缩桌面验收、启动阶段停止按钮和 DSH 压缩通知接入；不可将此项标记全面完成。

B2 压缩事件基础：确认原生压缩发生于 AgentExecution 创建前，因此增加 AgentPromptRequest.onCompactionProgress 向 CompactionRequest 的透传。LLM 压缩只在达到阈值、真正开始摘要时通知 started，finally 通知 finished，并区分 completed/fallback/failed/aborted；观察器异常不改变压缩结果。服务 8 项测试覆盖四结局、未达阈值无通知和观察器异常隔离。仍未连接 Web 启动传输及界面，不能声称“正在压缩”已展示；窗口式及 DSH 兼容压缩需分别接入。无版本变化或 EXE 重打。

B2：新增 PI 原生 runtime_activity preparing / waiting-model 事件。preparing 在 transformContext 钩子前发布，waiting-model 在请求配置和模型选择完成后发布，每次重试 prepare 也会发出，不依赖去重后的 request_header。前端保留准备状态直到真实等待边界，同一占位节点接收后续输出，回合结束清除摘要。四文件 66 项回归、全仓 typecheck、Web 构建及深色真实 PI 写入/审查回退/重开桌面场景通过。桌面场景是正常链路回归，不是所有静默阶段的精确时序验收。压缩、恢复、重试退避和等待子代理的统一阶段仍缺，B2 保持未完成；未重打 EXE。

E1 / D1：最新源码的历史桌面场景在 light 和 dark 各退出 0。场景使用真实持久化的 80 回合记录，实际点击搜索结果，展开旧回合思考、检查跨 Markdown 节点的 Highlight 范围和可见位置；仅搜索索引响应受控，不是索引全链路验收。新增颜色检查验证用户消息、状态卡片和展开搜索框的主题亮度及主要文字对比度 ≥4.5。修复普通卡片误用 overlay 颜色，以及搜索展开态写死深色。此证据不覆盖全部设置/弹层，E1 仍为部分；未重打 EXE。git diff --check 通过。

- E1 当前主题修改的深浅桌面验证已取得上述局部证据，继续收敛 B/C 运行链路；不要反复给每个小改动打 EXE。
- G5/G2/G3/G4 在一批功能收敛后集中验收。旧产物测试不能抵扣新代码验收。
- 每次更新报告引用 ID、发生的变化、实际证据和剩余条件。不得仅凭新增测试文件把项目标为完成。
- 外部条件暂不可用时写清缺什么，继续其他可执行项；本表没有已批准的暂缓项。
- 发现参考项目其他实质性缺口，登记并说明范围；不得默默遗漏，也不得把装饰差异无限扩大成新目标。
