# Seal Harness Agent Instructions

## 版本号与发布

- 默认只递增补丁版本号，即 `x.y.z` 中最后一位 `z`。例如当前版本为 `0.3.0` 时，
  下一次普通迭代应使用 `0.3.1`，然后是 `0.3.2`。
- 递增中间版本号 `y`（例如 `0.3.x` 升级到 `0.4.0`）属于较大版本更新。修改任何
  package manifest、发布工作流默认值、发布说明、Git 标签或 GitHub Release 之前，
  必须先向用户说明影响并取得明确同意。
- 递增主版本号 `x` 同样必须先取得用户明确同意。
- 不得仅根据功能规模自行推断应升级中间版本号或主版本号；没有明确同意时，一律使用
  下一个补丁版本号。
- 预发布版本必须沿用已经确认的基础版本号，例如 `0.3.1-candidate`，不能借预发布流程
  擅自提升中间版本号。
- 创建版本提交、标签或 Release 前，必须再次核对仓库内所有版本字段与最终版本号一致。
